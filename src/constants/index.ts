export enum SchemaTypesEnum {
  STRING = 'string',
  OBJECT = 'object',
  ARRAY = 'array',
  NUMBER = 'number',
  BOOLEAN = 'boolean',
  VOID = 'void',
  DATE = 'date',
  DATETIME = 'datetime',
}

export enum ConfigEnum {
  SELLER_INFORMATION = 0,
  BUSINESS_INFORMATION = 1,
  SELLER_REVIEW = 2,
}
